#version 330 core
layout (location = 0) in vec2 aPos; // Expects a screen quad from (-1,-1) to (1,1)

out vec2 ScreenPos;

void main() {
    ScreenPos = aPos;
    gl_Position = vec4(aPos, 0.0, 1.0);
}